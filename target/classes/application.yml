server:
  port: 8765
spring:
  application:
    name: api-gateway
  cloud:
    zookeeper:
      connect-string: localhost:2181
    #gateway:
      #discovery:
        #locator:
          #enabled: true